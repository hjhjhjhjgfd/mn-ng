<?xml version="1.0" encoding="utf-8"?>
<!--
  activity_gfw_privacy_policy - simple scrollable text view used
  by GFW_privacy_policy_Activity to render the privacy_policy.txt
  asset. Originally missing from the repo - added so the project
  compiles cleanly with GitHub Actions.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="?attr/colorMainBg"
    android:padding="@dimen/activity_horizontal_margin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView_privacy_policy"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:lineSpacingExtra="4dp"
            android:textColor="?attr/colorMainText"
            android:textIsSelectable="true"
            android:textSize="14sp" />

    </LinearLayout>
</ScrollView>
